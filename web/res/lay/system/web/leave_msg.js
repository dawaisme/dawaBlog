layui.config({
	base: '/res/lay/system/'
}).use(['layer', 'element', 'edit', 'flow'], function() {
	var element = layui.element,
		layer = layui.layer,
		$ = layui.$,
		edit = layui.edit,
		flow = layui.flow;


});